<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cibei.mapper.UserSettingsMapper">

    <resultMap id="UserSettingsResultMap" type="cibei.entity.UserSettings">
        <id     property="id"              column="id"/>
        <result property="userId"          column="user_id"/>
        <result property="targetQuitDate"  column="target_quit_date"/>
        <result property="nextDestination" column="next_destination"/>
        <result property="createdAt"       column="created_at"/>
        <result property="updatedAt"       column="updated_at"/>
    </resultMap>

    <!-- 新增 -->
    <insert id="insert" parameterType="cibei.entity.UserSettings">
        INSERT INTO user_settings (id, user_id, target_quit_date, next_destination, created_at, updated_at)
        VALUES (#{id}, #{userId}, #{targetQuitDate}, #{nextDestination}, #{createdAt}, #{updatedAt})
    </insert>

    <!-- 根据ID查询 -->
    <select id="selectById" parameterType="String" resultMap="UserSettingsResultMap">
        SELECT * FROM user_settings WHERE id = #{id}
    </select>

    <!-- 根据 userId 查询 -->
    <select id="selectByUserId" parameterType="String" resultMap="UserSettingsResultMap">
        SELECT * FROM user_settings WHERE user_id = #{userId}
    </select>

    <!-- 更新（动态，只更新非 null 字段） -->
    <update id="update" parameterType="cibei.entity.UserSettings">
        UPDATE user_settings
        <set>
            <if test="targetQuitDate  != null">target_quit_date  = #{targetQuitDate},</if>
            <if test="nextDestination != null">next_destination  = #{nextDestination},</if>
            updated_at = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <!-- 根据ID删除 -->
    <delete id="deleteById" parameterType="String">
        DELETE FROM user_settings WHERE id = #{id}
    </delete>

    <!-- 根据 userId 删除 -->
    <delete id="deleteByUserId" parameterType="String">
        DELETE FROM user_settings WHERE user_id = #{userId}
    </delete>

</mapper>
